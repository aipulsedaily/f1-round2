# Version control exists here for ONE reason: on 2026-08-03 an agent's edit sliced
# world/items/HUMAN-REFERENCE.md from a heading to end of file, 1,655 lines -> 538.
# There was no git, no backup, and the content was not recoverable from the
# transcript. Sections 000 and 1-7 -- the layer map, the LOD table, and the
# amplitude/frequency traps and guard rails, i.e. the "how to build a figure"
# half -- are PERMANENTLY LOST.
#
# So: track the SOURCE and the REASONING. Do not track the artefacts, which are
# large, regenerable, and would make commits useless.

# Renders, blends, caches, bakes -- all regenerable, all enormous
*.blend
*.blend1
*.blend2
*.png
*.jpg
*.exr
*.npz
*.wav
*.flac
*.mp4
# R2-4023: `*.mov` was missing while `*.mp4` was here, so the 11.25 GB ProRes
# delivery master sat untracked in watch/ with nothing but the never-`git add -A`
# convention stopping it being committed permanently. A convention is not a rule.
*.mov
*.bphys
# `render/*`, not `render/`: git will not descend into an excluded DIRECTORY,
# so the battery source below could never be re-included while this said
# `render/`. Everything at the top of render/ is still ignored.
render/*
work/
sim/tmp/
# R2-2233: `tmp/` was untracked AND unignored, and `gitguard.py seed-inflight`
# claims the whole dirty set -- so 160 of the 304 paths in the 2026-08-07 seed,
# 53% of it, were scratch files. Every one of them was a future false refusal
# aimed at whoever next touched that name. 86 entries, 733 MB, ZERO tracked
# files, so ignoring it loses nothing and shrinks the next seed to a fifth.
# The lesson is not about tmp/: a lock that claims "everything dirty" inherits
# the tree's untidiness as policy, and the guard had no way to tell scratch
# from somebody's unfinished work.
tmp/
__pycache__/
*.pyc
.venv/

# WHY THE ~210 UNTRACKED FILES BELOW `audio/out/`, `sim/out/`, `docs/` AND
# `tools/` ARE **NOT** IGNORED HERE, AND MUST NOT BE "FIXED" WITH A BLANKET RULE.
#
# A publication audit counted 212 files that are untracked AND unignored, and
# the obvious reflex — ignore `audio/out/`, `sim/out/`, `docs/*.json` — is
# WRONG in this repository. Measured before writing this comment:
#
#     audio/out/*        65 TRACKED files        sim/out/*      74 TRACKED
#     docs/*.json        27 TRACKED              docs/*.log      9 TRACKED
#     tools/*.py        251 TRACKED              world/items/*.py  45 TRACKED
#
# Every one of those classes is a class this project DELIBERATELY TRACKS. The
# evidence JSON and the run logs under `audio/out/` and `sim/out/` are promoted
# selectively, by hand, because they are the record of the run that produced a
# verdict — the same reasoning the `render/items/*/gate.json` block below is
# written on. A blanket ignore would not untrack them, but it would silently
# stop the NEXT one from ever being promoted, which is how the evidence trail
# dies quietly.
#
# And the split is not even mostly scratch: of the 212, **86 are `.py`/`.sh`/
# `.md` SOURCE** — item modules under `world/items/`, probes under `tools/`,
# sim code — several of which are referenced by `docs/item_manifest.json`.
# Ignoring those would hide files the manifest already points at, turning a
# "not committed yet" problem into a "structurally invisible" one. That is the
# exact failure this file's header is about.
#
# So the untracked set is an OWNER DECISION PASS — commit the source, delete or
# promote the evidence — and not an ignore-rule problem. What actually protects
# this tree is the rule that has always protected it: **`git add` is
# path-scoped, never `git add -A`, never `git add .`**. The blocks in this file
# cover the classes that are unambiguous (locks, credentials, cross-repo
# symlinks, artefacts); they deliberately stop short of the classes where
# ignoring would destroy the record.

# CREDENTIALS. This file matched NO secret file class at all until now.
#
# A secrets audit put `git check-ignore` against `.env`, `.env.local`, `.envrc`,
# `.netrc`, `*.pem`, `id_rsa`, `secrets.json`, `credentials.yml`, `api_key.txt`
# and `token.json`: every one came back NOT IGNORED. The audit also found no
# credential anywhere in this repository's history, so this block is preventive
# rather than remedial — and preventive is the only useful kind, because the
# remedy for a committed key is rotating it, not deleting it.
#
# It is easy to assume a render project has no credentials to leak. This tree
# drives a broker that rents billable hardware and reaches rented instances over
# SSH, so the keys are one careless `>` away at any time. `id_*` is wide on
# purpose: it catches `id_rsa`, `id_ed25519` and the `id_vast_render` key.
#
# Verified against `git ls-files` when added — no tracked file matches any
# pattern here. Re-check that before extending the list.
.env
.env.*
.envrc
.netrc
*.pem
*.key
id_*
*api_key*
*secret*
*token*
credentials*

# Build locks. Zero-byte mutexes held by whichever assembly is running, created
# and removed by the build itself -- pure machine state carrying no information
# at all, and they were untracked AND unignored, which is the R2-2233 shape one
# directory up: a file nobody would ever choose to commit, sitting in the dirty
# set waiting for the one `git add` that is not path-scoped. `.buildlock.q/` is
# a directory and git ignores empty ones, but it is named here so the rule does
# not quietly stop covering it the moment something lands inside.
.buildlock
.buildlock.*

# CROSS-REPO SYMLINKS INTO THE BROKER'S OUTPUT, AND WHY THEY MUST NOT SHIP.
#
# `watch/r2943_4k` and `watch/r2943b6_frames` are not directories. They are
# ABSOLUTE symlinks into ~/vast-render/out/seq/, which is a different
# repository whose `out/` is itself gitignored as regenerable 12 GB of frames.
#
# Git does not descend a symlink, so the *.png rule above never applied to
# them: they sat untracked and unignored as links, and committing one stores
# the LINK -- a blob containing an absolute path that exists on exactly one
# machine in the world. A clone gets a dangling pointer into a tree it does not
# have, which is worse than getting nothing, because a dangling link still
# looks like a delivered artefact until something tries to read it.
#
# The frames themselves are the broker's output and are regenerable there. If a
# specific frame needs to be kept as evidence, COPY it into the repo under a
# name that says what it proves; do not leave a link to somebody else's
# scratch directory standing in for it.
watch/r2943_4k
watch/r2943b6_frames

# Large generated data. The DOCS in docs/ are tracked; these are not.
docs/screen_presence*.json
docs/screen_presence*_objects.json
docs/*_points.npz
# R2-1099: `docs/beat_sheet.json` was ignored here, and that is the structural
# reason a beat-1 camera fix sat unpromoted for ten hours while FOUR *candidate*
# sheets sat tracked beside it. It is 157 KB, not "large generated data", and it
# is not merely an output — it is the input `build_camera_rig` and
# `build_car_anim` read. The versioned artefact must be the one that ships.
docs/collision_report.json
docs/placement*.json
docs/depth_probe.json
docs/instance_variety.json
docs/presentation_normals.json
docs/inventory_iter.json

# THE GATE BATTERY IS SOURCE, AND `render/` WAS SWALLOWING IT.
#
# This file's own header says the point of version control here is to track THE
# SOURCE AND THE REASONING, because an agent's edit destroyed 1,655 lines that
# were not recoverable. But `render/` and `work/` are blanket-ignored, and real,
# hand-written, heavily-reasoned source lives in both:
# render/world/assembly/r2/ holds assemble.py, probeA..probeK, lib_battery.sh,
# the three battery.sh files and make_controls.py -- the entire #53 gate battery
# and every control scene generator in the project.
#
# The consequence is not hypothetical. `git log` on any of them is empty, so
# "establish a baseline from committed state before you change anything"
# (R2-079) is structurally IMPOSSIBLE for the battery, and the destruction this
# file exists to prevent is exactly as possible there today as it was before git
# was introduced. The .blend/.png/.exr rules above still exclude the artefacts;
# these two lines let the SOURCE through.
# Git cannot re-include a file whose parent DIRECTORY is excluded, so every
# level has to be opened one at a time. Everything not named here stays ignored,
# and the *.blend / *.png / *.exr rules above still win over these.
!render/world/
render/world/*
!render/world/assembly/
render/world/assembly/*
!render/world/assembly/r2/
render/world/assembly/r2/*
!render/world/assembly/r2/*.py
!render/world/assembly/r2/*.sh
# The three directories below were named ONE AT A TIME, so v123 -- which holds
# verify_assembly8.sh, the script SHIPPING.md tells the reader to run, plus
# material_graph_census.py, material_graph_diff.py, path_diff.py and
# build_json_diff.py -- was still fully ignored and has no history at all.  That
# is the exact failure this file's header was written about, one directory over.
# The glob covers every vNNN batch from here on; the `*` line below still
# excludes everything in them that is not .py or .sh.
!render/world/assembly/r2/v12?/
!render/world/assembly/r2/v1??/
render/world/assembly/r2/v1??/*
!render/world/assembly/r2/v1??/*.py
!render/world/assembly/r2/v1??/*.sh

# THE ITEM GATE VERDICTS ARE A STAGE-1 BUILD INPUT, AND `render/*` ATE THEM.
# R2-4020, found by the sweep that followed R2-3961.
#
# `world/build_items.py::check_row()` reads `render/items/<item>/gate.json` and,
# because `require_gate_accepted` defaults TRUE, appends a FATAL unless it finds
# `"result": "ITEM_ACCEPTED"`:
#
#     gate verdict is None, not ITEM_ACCEPTED (read from ...). A placement
#     stage that ships un-accepted geometry makes the gate optional.
#
# So `rm -rf render/items/` does not degrade the world build, it STOPS it --
# exactly the position `work/r2_1211_rubber_tracks.json` was in, one directory
# over, and for the same reason: a blanket `render/*` written to exclude
# renders swallowed the evidence sitting among them.
#
# AND THEY ARE NOT REGENERABLE IN THE WAY THAT MATTERS.  `tools/item_gate.py`
# will write a gate.json, so "a script produces it" is true and beside the
# point: what it writes is the verdict of A NEW RUN.  This project's own
# `tools/r2_1381_rescore.py` says it plainly -- "`gate.json` files are the
# record of the run that produced them and are not edited after the fact".
# The 33 files here are the record of the runs that ACCEPTED THE SHIPPED
# GEOMETRY. Re-rendering cannot restore that; it can only replace it with
# something else that has the same name.  520 KB for 33 files, so the "large,
# regenerable artefacts" reasoning at the top of this file does not apply.
#
# Git cannot re-include a file whose parent DIRECTORY is excluded, so every
# level is opened one at a time and immediately re-closed.  Nothing else under
# `render/items/` -- the gate renders, the crops, the EXRs -- becomes visible,
# and the *.png / *.exr / *.blend rules above still win inside it.
!render/items/
render/items/*
!render/items/*/
render/items/*/*
!render/items/*/gate.json

# Telemetry is generated from the spec, but it IS the single source of truth for
# motion AND audio and it has been wrong in ways that took a day to find. Track it.
!telemetry/telemetry.csv

# THE BAR'S OWN INSTRUMENTS WERE UNTRACKED.  R2-2821.
#
# `work/` is blanket-ignored above, and the two probes the film bar reads every
# one of its numbers from live in it:
#
#     work/lighting/measure_film_scene.py    the delivery format, the grade,
#                                            the lamp load, the frame range
#     work/r2100/measure_film_extra.py       the levelling identity, the camera
#
# So `git log` on either is empty, and the reason five of the bar's lines were
# decorative for four film generations -- `measure_film_scene.py` not emitting
# `resolution_x` -- is a change nobody could have bisected, reviewed or
# reverted.  That is the SAME failure this file's header is about (1,655 lines
# destroyed with no history) and the same one the `render/world/assembly/r2/`
# block above was written to close, one directory over.
#
# Git cannot re-include a file whose parent DIRECTORY is excluded, so every
# level is opened one at a time and immediately re-closed.  Nothing else in
# `work/` -- gigabytes of blends, logs and scratch -- becomes visible.
!work/
work/*
!work/lighting/
work/lighting/*
!work/lighting/measure_film_scene.py
!work/r2100/
work/r2100/*
!work/r2100/measure_film_extra.py

# THE 2026-08-18 PUBLICATION PASS: WHAT WAS DECIDED ABOUT THE 211.
#
# The block above says the untracked set is an OWNER DECISION PASS and not an
# ignore-rule problem, and warns that a blanket rule over `audio/out/`,
# `sim/out/`, `docs/*.json` or `tools/` would be WRONG because every one of
# those is a class this project deliberately tracks. That reasoning held up.
# The pass went file by file and resolved 162 of the 211:
#
#     84 committed as SOURCE     (b618ca1) -- every hand-written .py/.sh/.md
#     78 promoted as EVIDENCE    (the commit above) -- named verdicts only
#      2 ignored                 (this block)
#     47 deliberately LEFT ALONE (untracked AND unignored -- see the bottom)
#
# Only TWO classes earned an ignore rule, and both had to clear the same bar:
# ignoring it must lose nothing that could not be recreated identically.
#
# 1. STEM BOUNCES. `audio/out/**/stems/` holds STEMS_OF.json and
#    render_stems.log, one set per audio render. This repository has tracked
#    ZERO files matching `stems` in its entire history -- checked with
#    `git log --all --diff-filter=A --name-only | grep -c stems` -> 0 -- so
#    this is not a class that is promoted selectively, it is a class that has
#    never once been promoted. There is nothing here for a blanket rule to kill
#    off, which is exactly the test the block above sets.
audio/out/**/stems/
audio/out/**/stems_smoke/
#
# 2. `world/items/grandstand_seats.json`, 2.6 MB, the single largest untracked
#    file. It is the OUTPUT of tracked `world/items/grandstand_seats.py`,
#    written by that script through its own `OUT_DEFAULT` from a SEEDED RNG. So
#    unlike `render/items/*/gate.json`, re-running does not produce "the
#    verdict of a new run" -- it produces the same bytes. Regenerable in the
#    strong sense, which is the only sense that justifies ignoring something.
world/items/grandstand_seats.json
#
# WHAT WAS DELIBERATELY *NOT* IGNORED, AND WHY THAT IS NOT AN OVERSIGHT.
#
# 47 files are still untracked and still unignored after this pass. They were
# looked at and left, not missed:
#
#   * ~19 per-run `render.log` / `verify.log` / `matrix_*.log` under
#     `audio/out/*/`. The repository DOES track 20 `audio/out/*.log` files, so
#     logs here are promoted selectively and a blanket `*.log` rule would
#     silently stop the next promotion. Eleven near-identical `render.log`s are
#     not worth keeping; the rule that would hide them is worth less.
#   * ~16 `world/*.json` baked animation and camera-path snapshots, named
#     `R2829_*`, `R21701_*`, `R2_3361_*`, `*_PRE_R2943_*`, `*_base_*`,
#     `verify_world_a9_*`. Their current-generation siblings ARE tracked
#     (`car_anim_car.json`, `camera_rig_path.json`, and now `car_anim_imp.json`
#     and `car_anim_driver_imp.json`). These are per-experiment snapshots, and
#     a pattern narrow enough to catch them without also catching the tracked
#     generation would have to be maintained by hand for every future R-number.
#   * `world/_b6_before_terrain.py` (241 KB), a "before" snapshot wearing a
#     `.py` extension, referenced by nothing tracked.
#   * `docs/instance_variety_SUPERSEDED_pre_R2_3721.json`, which says what it
#     is in its own name.
#
# All 47 were scanned for credentials, home paths, emails and routable IPs
# before this decision -- they are clean, so leaving them untracked risks
# nothing being published. What protects them from a careless commit is what
# has always protected this tree: **`git add` is path-scoped, never
# `git add -A`, never `git add .`** -- now enforced by `tools/gitguard.py`
# rather than only asserted.

# THE DE-ALIASING TABLE. THE ONE FILE IN THIS TREE THAT MUST NEVER BE TRACKED.
#
# docs/PUBLICATION-AUDIT.md §4 -- "The one real finding: the sanitiser ships its
# own de-aliasing table" -- found THREE REAL ROUTABLE IP ADDRESSES of rented
# third-party GPU hosts, plus eighteen real vast.ai machine ids, sitting as
# source literals in tools/publication/sanitise_docs.py. That file is tracked.
# The documentation was correctly aliased to `host-A` and `mach-NN`, and it did
# not matter: shipping the sanitiser shipped the lookup table that undoes it,
# and anyone with the repository could invert every alias in every document by
# reading two lines of Python.
#
# The sibling repository had already caught the identical shape, about
# farm/hostrates.json: "Anyone holding both files joins them on those values and
# recovers every identifier the aliasing was for. While this file was tracked,
# the docs' redaction was cosmetic." It fixed it the same way -- its d056d4ba is
# literally "untrack the table that de-aliases the docs".
#
# So the VALUES moved here and the ALIASES stayed in the tool. A clone gets the
# method, the reasoning and the vocabulary, and no way to invert any of it.
#
# UNTRACKED RATHER THAN GENERICISED, deliberately: replacing the addresses with
# placeholders in the tool would leave a map that still looks authoritative and
# silently maps nothing. Absence is legible; a wrong table is not.
#
# Losing this file is safe, which is the only thing that makes ignoring it
# defensible. The sanitiser needs it to SUBSTITUTE, not to DETECT: MACH_SHAPE
# and IP_SHAPE still fire on any unknown machine id or non-benign address and
# still report it, which is the check that protects a NEW document and the one
# that caught a planted 203.0.113.77 canary. Without the map the tool says so on
# stderr and declines to rewrite; it does not quietly pass anything through.
tools/publication/host_canon.txt
